# Revenue Share 20-80 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Change the platform revenue split to `admin 20% / instructor 80%` across checkout, admin reporting, instructor reporting, tests, and API docs.

**Architecture:** Add one shared backend revenue-share constant module, then route checkout writes and reporting calculations through that single source of truth. Keep response DTOs and frontend contracts unchanged so the change is business-logic only.

**Tech Stack:** NestJS, TypeScript, TypeORM SQL queries, Jest

---

### Task 1: Add shared revenue-share constants

**Files:**
- Create: `backend/src/common/constants/revenue-share.ts`

- [ ] **Step 1: Create the shared constant module**

```typescript
export const ADMIN_REVENUE_SHARE = 0.2;
export const INSTRUCTOR_REVENUE_SHARE = 0.8;

export const ADMIN_REVENUE_PERCENT = ADMIN_REVENUE_SHARE * 100;
export const INSTRUCTOR_REVENUE_PERCENT = INSTRUCTOR_REVENUE_SHARE * 100;
```

### Task 2: Update tests first

**Files:**
- Modify: `backend/src/modules/admin/admin.service.spec.ts`
- Modify: `backend/src/modules/instructors/instructors.service.spec.ts`
- Create: `backend/src/modules/checkout/checkout.service.spec.ts`

- [ ] **Step 1: Rewrite admin dashboard expectations for 20/80**
- [ ] **Step 2: Rewrite instructor reports expectations for 20/80**
- [ ] **Step 3: Add a checkout regression test that asserts `TiLeGiangVien = 80` and `DoanhThuGiangVien` uses 80%**
- [ ] **Step 4: Run the targeted backend tests and confirm they fail before implementation**

Run: `rtk proxy powershell -NoProfile -Command "npm test -- admin.service.spec.ts instructors.service.spec.ts checkout.service.spec.ts --runInBand"` in `backend`

Expected: FAIL because production code still uses the old split.

### Task 3: Update backend logic

**Files:**
- Modify: `backend/src/modules/checkout/checkout.service.ts`
- Modify: `backend/src/modules/admin/admin.service.ts`
- Modify: `backend/src/modules/instructors/services/instructors.service.ts`

- [ ] **Step 1: Replace hardcoded checkout rate with shared constants**
- [ ] **Step 2: Replace admin dashboard 60/40 SQL literals with shared constants**
- [ ] **Step 3: Replace instructor reports 60/40 SQL literals with shared constants**
- [ ] **Step 4: Re-run the targeted tests and confirm they pass**

Run: `rtk proxy powershell -NoProfile -Command "npm test -- admin.service.spec.ts instructors.service.spec.ts checkout.service.spec.ts --runInBand"` in `backend`

Expected: PASS

### Task 4: Update docs and verify

**Files:**
- Modify: `docs/api-contract.md`

- [ ] **Step 1: Update API contract wording from 60/40 to 20/80**
- [ ] **Step 2: Run backend lint**
- [ ] **Step 3: Run targeted backend tests again as final verification**

Run: `rtk proxy powershell -NoProfile -Command "npm run lint"` in `backend`
Expected: PASS

Run: `rtk proxy powershell -NoProfile -Command "npm test -- admin.service.spec.ts instructors.service.spec.ts checkout.service.spec.ts --runInBand"` in `backend`
Expected: PASS
